# Model and API Configuration for PaperVizAgent

# Default Models
defaults:
  model_name: "gemini-2.5-flash"
  image_model_name: "nano-banana-2"

# API Keys (leave empty — use environment variables on Railway)
api_keys:
  kie_api_key: ""
  openai_api_key: ""
  anthropic_api_key: ""
